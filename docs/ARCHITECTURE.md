# Legacy Doctor Architecture

Legacy Doctor is a deterministic standalone instrument.

Components:

Device enumeration layer  
Device identity layer  
Execution layer  
Receipt layer  

No external dependencies required.
